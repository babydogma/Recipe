/* Portionly shared UI ownership map.
   Runtime is bundled in js/app.js for static GitHub Pages loading. */
window.PortionlyModuleMap = window.PortionlyModuleMap || {};
window.PortionlyModuleMap.ui = {
  responsibilities: ["toast", "copy fallback", "image preload", "formatting helpers", "HTML escaping"]
};
